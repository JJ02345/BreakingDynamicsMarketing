// Re-export from new squads/core location for backwards compatibility
export {
  LanguageProvider,
  useLanguage,
  LanguageSwitcher,
} from '../squads/core/providers/LanguageProvider';

// Default export for backward compatibility
export { default } from '../squads/core/providers/LanguageProvider';
