// Re-export from new squads/auth location for backwards compatibility
export {
  AuthProvider,
  useAuth,
} from '../squads/auth/AuthProvider';

// Default export for backward compatibility
export { default } from '../squads/auth/AuthProvider';
