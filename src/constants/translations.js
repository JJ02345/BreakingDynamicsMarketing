// Re-export from modular structure for backward compatibility
export { en, de, es, fr, translations } from './translations/index';

import { translations } from './translations/index';
export default translations;
