import { de } from './de';
import { en } from './en';
import { es } from './es';
import { fr } from './fr';

export const translations: Record<string, typeof en> = {
  de,
  en,
  es,
  fr,
};

export { de, en, es, fr };
