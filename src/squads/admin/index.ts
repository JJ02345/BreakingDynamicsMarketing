// Admin Squad - Public API
// Exports all public interfaces for admin dashboard

// Types
export type {
  FeedbackType,
  FeedbackStatus,
  FeedbackPriority,
  Feedback,
  AdminStats,
  AdminDashboardData,
  AdminUser,
  AdminContextValue,
} from './types';

// Note: Components will be migrated incrementally
// For now, import directly from src/components/admin/*
// and src/AdminDashboard.jsx
